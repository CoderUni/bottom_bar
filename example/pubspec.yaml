name: bottom_bar_example
description: An example app that shows how to use the bottom_bar package.

publish_to: 'none'
version: 1.0.0

environment:
  sdk: '>=2.12.0 <3.0.0'

dependencies:
  flutter:
    sdk: flutter

dev_dependencies:
  bottom_bar:
    path: ../

flutter:
  uses-material-design: true